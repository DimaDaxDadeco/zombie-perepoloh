// Заглушка звука для прогонов в Node.
//
// Round и сущности зовут у audio полтора десятка методов — boom(), medal(),
// hurt(), setBossMode() и прочие. Перечислять их поимённо значит завести
// список, который начнёт отставать от игры при первом же новом звуке, причём
// молча: незнакомый метод упадёт с TypeError посреди раунда.
//
// Proxy отвечает пустой функцией на что угодно, поэтому не отстаёт никогда.
export function createSilentAudio() {
  return new Proxy({}, { get: () => () => {} });
}
